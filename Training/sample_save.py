# the model referred to in this code is the model used in the training and validation loop, the second argument should be changed to the desired save location.
torch.save(model, "resnet50_complete_model3_UAV.pth")
